---
import path from 'node:path';
import DocumentFolderList from './DocumentFolderList.astro';

const publicDir = path.join(process.cwd(), 'public');
const folders = [
  {
    title: '正式组会',
    dir: path.join(publicDir, 'group-meetings', '正式组会'),
    url: '/group-meetings/正式组会',
  },
  {
    title: '自主组会',
    dir: path.join(publicDir, 'group-meetings', '自主组会'),
    url: '/group-meetings/自主组会',
    children: [
      {
        title: '光纤仿生传感',
        dir: path.join(publicDir, 'group-meetings', '自主组会', '光纤仿生传感'),
        url: '/group-meetings/自主组会/光纤仿生传感',
      },
      {
        title: '其他交叉领域',
        dir: path.join(publicDir, 'group-meetings', '自主组会', '其他交叉领域'),
        url: '/group-meetings/自主组会/其他交叉领域',
      },
    ],
  },
];
---

<DocumentFolderList folders={folders} />
